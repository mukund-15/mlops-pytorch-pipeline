model:
  architecture: resnet18
  num_classes: 10

training:
  epochs: 5
  batch_size: 64
  learning_rate: 0.001
  early_stopping_patience: 2
  num_workers: 2
  seed: 42

data:
  dataset: cifar10
  data_dir: data

output:
  checkpoint_dir: checkpoints
  model_name: classifier_v1.pt
