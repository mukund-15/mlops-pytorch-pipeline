apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mlops-storage
  namespace: ml-training
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: hostpath
  resources:
    requests:
      storage: 5Gi
